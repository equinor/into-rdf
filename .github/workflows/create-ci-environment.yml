name: ⚗️ Create Ci Environment
on:
  workflow_dispatch:
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: '🐛 Debug'
        if: contains(github.event.comment.body, 'byggekjør')
        run: echo 'Magic word was entered'
