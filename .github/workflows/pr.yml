name: ➕ Pull Request
on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build_test:
    name: "💎 Code Quality"
    uses: equinor/spinereviewapi/.github/workflows/codequality.yml@main
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
